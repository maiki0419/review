 <% if book.favorited_by?(current_user) %>
    <%= link_to "💙️",book_favorites_path(book.id),method: :delete,remote: true %><%= book.favorites.count %>
  <% else %>
    <%= link_to "♡", book_favorites_path(book.id),method: :post,remote: true %><%= book.favorites.count %>
  <% end %>